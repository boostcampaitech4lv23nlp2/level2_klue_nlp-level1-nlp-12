# pre-commit 적용 순서
# 1. git init
# 2. pip install pre-commit
# 3. pre-commit install
# 4. pre-commit run

# git add .
# git commit -m 'test'
## config 파일을 통해 black, isort 실행되어 적용됨
## 다시 git add와 commit을 실행하면 black,isort가 적용된 파일로 commit됨
